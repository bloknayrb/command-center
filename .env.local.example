# Command Center — Environment Variables
# Copy this file to .env.local and fill in your values

# Anthropic API Key (optional — omit to use Claude Code subprocess with Max subscription)
# ANTHROPIC_API_KEY=sk-ant-...

# Obsidian Vault Path (required)
OBSIDIAN_VAULT_PATH=/path/to/your/obsidian/vault

# SimpleMem MCP (optional — degrades gracefully)
# SIMPLEMEM_PATH=/path/to/simplemem-mcp/dist/index.js
# SIMPLEMEM_DB_PATH=/path/to/simplemem/data/lancedb_data
# SIMPLEMEM_PYTHON=/path/to/simplemem/venv/bin/python

# OpenMemory MCP (optional — degrades gracefully)
# OPENMEMORY_URL=http://localhost:8081

# PIP Evidence (optional — enables PIP dashboard panel + agent tool)
# NEXT_PUBLIC_ENABLE_PIP=true
